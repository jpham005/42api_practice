export interface User {
  id: number;
  email: string;
  login: string;
  correctionPoint: number;
  poolYear: number;
  poolMonth: number;
  wallet: number;
  active: boolean;
}
// updatedAt: string;
